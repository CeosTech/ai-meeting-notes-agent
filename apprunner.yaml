version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r ai-meeting-notes-agent/requirements.txt
run:
  runtime-version: 3.11.9
  command: /usr/bin/python3 -m uvicorn backend.app:app --host 0.0.0.0 --port 8080 --app-dir ai-meeting-notes-agent
  network:
    port: 8080
